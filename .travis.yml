language: c
compiler: gcc

addons:
  apt:
    update: true
    packages:
      - make
      - libeditline-dev
      - libreadline-dev
      - libedit-dev

script: make

notifications:
  email: false

