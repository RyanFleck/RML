language: c
compiler: gcc

addons:
  apt:
    update: true
    packages:
      - build-essential
      - make
      - gcc
      - libreadline 
      - libeditline-dev
      - libreadline-dev
      - libedit-dev

script: make

notifications:
  email: false

